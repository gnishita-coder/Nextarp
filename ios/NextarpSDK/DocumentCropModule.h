// Unused - inert stub (never added to the Xcode project/target, so this is
// just cleanup, not a build risk). This was a native OpenCV post-capture
// crop module, built while camera-kit was the active capture library. No
// longer needed now that capture uses @dariyd/react-native-document-scanner
// (VisionKit / ML Kit), which does its own, more accurate native detection.
