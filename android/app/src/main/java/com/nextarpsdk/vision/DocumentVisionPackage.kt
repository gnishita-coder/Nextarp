package com.nextarpsdk.vision

// Unused - kept as an inert stub file (can't be deleted in this sandbox
// environment). See DocumentCropModule.kt for why: the native OpenCV
// post-capture crop step was removed in favor of
// @dariyd/react-native-document-scanner's own native detection. No longer
// registered in MainApplication.kt.
