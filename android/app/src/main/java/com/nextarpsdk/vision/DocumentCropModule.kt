package com.nextarpsdk.vision

// Unused - kept as an inert stub file (can't be deleted in this sandbox
// environment). This was a native OpenCV post-capture crop module, built
// while camera-kit was the active capture library. It's no longer needed:
// we've switched capture to @dariyd/react-native-document-scanner
// (Apple VisionKit / Google ML Kit), which does its own, more accurate,
// ML-based document detection + perspective correction natively - see
// App.tsx's header comment for the full history. The OpenCV Gradle/Podfile
// dependencies have also been removed, so this file intentionally has no
// OpenCV imports left (they would no longer resolve).
